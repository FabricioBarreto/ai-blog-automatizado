---
import AffiliateDisclaimer from "./AffiliateDisclaimer.astro";
import CookieConsent from "./CookieConsent.astro";

const currentYear = new Date().getFullYear();

const footerLinks = {
	resources: [
		{ label: 'Todos los Artículos', href: '/blog' },
		{ label: 'Acerca de Nosotros', href: '/about' },
		{ label: 'Newsletter', href: '#newsletter' },
		{ label: 'RSS Feed', href: '/rss.xml' },
	],
	categories: [
		{ label: 'Herramientas de IA', href: '/blog?category=ia' },
		{ label: 'Productividad', href: '/blog?category=productividad' },
		{ label: 'Tutoriales', href: '/blog?category=tutoriales' },
		{ label: 'Reviews', href: '/blog?category=reviews' },
	],
	legal: [
		{ label: 'Política de Privacidad', href: '/privacy' },
		{ label: 'Términos de Uso', href: '/terms' },
		{ label: 'Política de Afiliados', href: '/affiliate-policy' },
		{ label: 'Política de Cookies', href: '/cookies' }, 
		{ label: 'Contacto', href: '/contact' },            
	]
};

const socialLinks = [
	{ icon: '𝕏', label: 'Twitter', href: 'https://twitter.com' },
	{ icon: 'in', label: 'LinkedIn', href: 'https://linkedin.com' },
	{ icon: '▶', label: 'YouTube', href: 'https://youtube.com' },
	{ icon: 'RSS', label: 'RSS Feed', href: '/rss.xml' },
];
---

<style>
	footer {
		background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
		color: rgba(255, 255, 255, 0.9);
		margin-top: 6rem;
		position: relative;
		overflow: hidden;
	}

	footer::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(90deg, 
			transparent 0%, 
			rgba(102, 126, 234, 0.5) 50%, 
			transparent 100%
		);
	}

	.footer-content {
		max-width: 1200px;
		margin: 0 auto;
		padding: 4rem 2rem 2rem;
		position: relative;
	}

	.footer-top {
		display: grid;
		grid-template-columns: 1.5fr 1fr 1fr 1fr;
		gap: 3rem;
		margin-bottom: 3rem;
		padding-bottom: 3rem;
		border-bottom: 1px solid rgba(255, 255, 255, 0.1);
	}

	.footer-about {
		max-width: 320px;
	}

	.footer-logo {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		margin-bottom: 1.5rem;
		font-size: 1.5rem;
		font-weight: 800;
	}

	.footer-logo-icon {
		font-size: 2rem;
		filter: drop-shadow(0 2px 8px rgba(102, 126, 234, 0.5));
	}

	.footer-logo-text {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.footer-about p {
		line-height: 1.7;
		margin-bottom: 1.5rem;
		color: rgba(255, 255, 255, 0.7);
		font-size: 0.95rem;
	}

	.social-links {
		display: flex;
		gap: 0.75rem;
		flex-wrap: wrap;
	}

	.social-link {
		width: 44px;
		height: 44px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgba(255, 255, 255, 0.08);
		border-radius: 12px;
		color: white;
		text-decoration: none;
		font-size: 1.1rem;
		font-weight: 700;
		transition: all 0.3s ease;
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.1);
	}

	.social-link:hover {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		transform: translateY(-3px);
		box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
	}

	.footer-section h3 {
		color: white;
		font-size: 1.1rem;
		margin-bottom: 1.5rem;
		font-weight: 700;
		letter-spacing: -0.3px;
	}

	.footer-section ul {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.footer-section li {
		margin-bottom: 0.75rem;
	}

	.footer-section a {
		color: rgba(255, 255, 255, 0.65);
		text-decoration: none;
		transition: all 0.3s ease;
		display: inline-block;
		font-size: 0.95rem;
	}

	.footer-section a:hover {
		color: white;
		transform: translateX(4px);
	}

	.footer-bottom {
		display: flex;
		justify-content: space-between;
		align-items: center;
		flex-wrap: wrap;
		gap: 2rem;
		padding-top: 2rem;
		color: rgba(255, 255, 255, 0.5);
		font-size: 0.9rem;
	}

	.footer-copy {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.footer-legal {
		display: flex;
		gap: 1.5rem;
		flex-wrap: wrap;
	}

	.footer-legal a {
		color: rgba(255, 255, 255, 0.6);
		text-decoration: none;
		transition: color 0.3s ease;
	}

	.footer-legal a:hover {
		color: white;
	}

	.footer-badge {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		background: rgba(102, 126, 234, 0.15);
		color: #a5b4fc;
		padding: 0.4rem 1rem;
		border-radius: 50px;
		font-size: 0.85rem;
		font-weight: 600;
		border: 1px solid rgba(102, 126, 234, 0.3);
	}

	/* Modern Back to Top Button with 3D effect and animations */
	.back-to-top {
		position: fixed;
		bottom: 2rem;
		right: 2rem;
		width: 56px;
		height: 56px;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
		border: none;
		border-radius: 16px;
		font-size: 1.4rem;
		cursor: pointer;
		opacity: 0;
		visibility: hidden;
		transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		z-index: 999;
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 
			0 4px 20px rgba(102, 126, 234, 0.3),
			0 0 0 0 rgba(102, 126, 234, 0.5);
		animation: pulse-ring 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
		backdrop-filter: blur(10px);
	}

	.back-to-top::before {
		content: '';
		position: absolute;
		inset: -2px;
		background: linear-gradient(135deg, #667eea, #764ba2);
		border-radius: 18px;
		z-index: -1;
		opacity: 0;
		transition: opacity 0.3s ease;
	}

	.back-to-top::after {
		content: '';
		position: absolute;
		inset: 2px;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		border-radius: 14px;
		z-index: -1;
	}

	.back-to-top.visible {
		opacity: 1;
		visibility: visible;
	}

	.back-to-top:hover {
		transform: translateY(-8px) scale(1.05);
		box-shadow: 
			0 8px 30px rgba(102, 126, 234, 0.5),
			0 0 0 8px rgba(102, 126, 234, 0.2);
	}

	.back-to-top:hover::before {
		opacity: 1;
	}

	.back-to-top:active {
		transform: translateY(-6px) scale(1.02);
	}

	.back-to-top-icon {
		display: flex;
		align-items: center;
		justify-content: center;
		transition: transform 0.3s ease;
	}

	.back-to-top:hover .back-to-top-icon {
		transform: translateY(-3px);
		animation: bounce 0.6s ease infinite;
	}

	/* Progress ring around button */
	.back-to-top-progress {
		position: absolute;
		inset: -4px;
		border-radius: 18px;
		border: 3px solid transparent;
		border-top-color: rgba(255, 255, 255, 0.8);
		border-right-color: rgba(255, 255, 255, 0.5);
		transition: transform 0.3s ease;
		pointer-events: none;
	}

	@keyframes pulse-ring {
		0%, 100% {
			box-shadow: 
				0 4px 20px rgba(102, 126, 234, 0.3),
				0 0 0 0 rgba(102, 126, 234, 0.5);
		}
		50% {
			box-shadow: 
				0 4px 25px rgba(102, 126, 234, 0.4),
				0 0 0 8px rgba(102, 126, 234, 0);
		}
	}

	@keyframes bounce {
		0%, 100% {
			transform: translateY(-3px);
		}
		50% {
			transform: translateY(-8px);
		}
	}

	@media (max-width: 1024px) {
		.footer-top {
			grid-template-columns: 1fr 1fr;
		}

		.footer-about {
			grid-column: 1 / -1;
			max-width: 100%;
		}
	}

	@media (max-width: 640px) {
		.footer-content {
			padding: 3rem 1.5rem 2rem;
		}

		.footer-top {
			grid-template-columns: 1fr;
			gap: 2.5rem;
		}

		.footer-bottom {
			flex-direction: column;
			text-align: center;
		}

		.footer-legal {
			justify-content: center;
		}

		.back-to-top {
			bottom: 1.5rem;
			right: 1.5rem;
			width: 50px;
			height: 50px;
			font-size: 1.3rem;
		}
	}
</style>

<footer>
	<div class="footer-content">
		<div class="footer-top">
			<!-- About Section -->
			<div class="footer-about">
				<div class="footer-logo">
					<span class="footer-logo-icon">🤖</span>
					<span class="footer-logo-text">AI Tools Hub</span>
				</div>
				<p>
					Reviews honestas, tutoriales prácticos y comparativas profundas de las mejores 
					herramientas de IA para impulsar tu productividad en 2025.
				</p>
				<div class="social-links">
					{socialLinks.map(link => (
						<a 
							href={link.href} 
							class="social-link" 
							aria-label={link.label}
							target="_blank"
							rel="noopener noreferrer"
						>
							{link.icon}
						</a>
					))}
				</div>
			</div>

			<!-- Resources -->
			<div class="footer-section">
				<h3>Recursos</h3>
				<ul>
					{footerLinks.resources.map(link => (
						<li>
							<a href={link.href}>{link.label}</a>
						</li>
					))}
				</ul>
			</div>

			<!-- Categories -->
			<div class="footer-section">
				<h3>Categorías</h3>
				<ul>
					{footerLinks.categories.map(link => (
						<li>
							<a href={link.href}>{link.label}</a>
						</li>
					))}
				</ul>
			</div>

			<!-- Legal -->
			<div class="footer-section">
				<h3>Legal</h3>
				<ul>
					{footerLinks.legal.map(link => (
						<li>
							<a href={link.href}>{link.label}</a>
						</li>
					))}
				</ul>
			</div>
		</div>

		<div class="footer-bottom">
			<div class="footer-copy">
				<span>&copy; {currentYear} AI Tools & Productivity Hub</span>
				<span class="footer-badge">
					🤖 Powered by AI
				</span>
			</div>
			<div class="footer-legal">
				<span>Hecho con ❤️ en Argentina</span>
			</div>
		</div>	
		<AffiliateDisclaimer placement="bottom" />
	</div>
</footer>
<CookieConsent />

<!-- Enhanced Back to Top Button -->
<button 
	class="back-to-top" 
	id="back-to-top" 
	aria-label="Volver arriba"
	title="Volver arriba"
>
	<div class="back-to-top-progress" id="progress-ring"></div>
	<span class="back-to-top-icon">↑</span>
</button>

<script>
	// Enhanced back to top functionality with scroll progress
	const backToTopButton = document.getElementById('back-to-top');
	const progressRing = document.getElementById('progress-ring');
	
	function updateScrollProgress() {
		const windowHeight = window.innerHeight;
		const documentHeight = document.documentElement.scrollHeight - windowHeight;
		const scrolled = window.pageYOffset;
		const progress = (scrolled / documentHeight) * 360;
		
		if (progressRing) {
			progressRing.style.transform = `rotate(${progress}deg)`;
		}
		
		if (scrolled > 500) {
			backToTopButton?.classList.add('visible');
		} else {
			backToTopButton?.classList.remove('visible');
		}
	}
	
	window.addEventListener('scroll', updateScrollProgress, { passive: true });

	backToTopButton?.addEventListener('click', () => {
		window.scrollTo({
			top: 0,
			behavior: 'smooth'
		});
	});

	// Keyboard accessibility
	backToTopButton?.addEventListener('keydown', (e) => {
		if (e.key === 'Enter' || e.key === ' ') {
			e.preventDefault();
			window.scrollTo({
				top: 0,
				behavior: 'smooth'
			});
		}
	});

	// Initialize on load
	updateScrollProgress();
</script>